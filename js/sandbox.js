/* ==========================================================================
    sandbox -- Version: 0.2.2 - Updated: 7/26/2014
    ========================================================================== */

    $(function() {
      $("input[type='radio']").click(function() {
        var cat = $(this).val();

        $(".hidden").hide();
        $(".category" + cat).fadeIn(500);
      });
    });
